CREATE TABLE usuarios (
    usuario_id BIGINT NOT NULL GENERATED BY DEFAULT AS IDENTITY,
    nombre VARCHAR(30) NOT NULL,
    email VARCHAR(60) NOT NULL UNIQUE,
    clave VARCHAR(200) NOT NULL,
    PRIMARY KEY (usuario_id)
);
